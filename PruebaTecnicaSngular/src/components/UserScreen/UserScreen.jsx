import styles from './UserScreen.module.css';

export default function UserScreen() {
    return (
        <div className={styles.divUser}>
            <h1>Aqui el usuario escoge el número</h1>
        </div>
    );
}